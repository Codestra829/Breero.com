import type { Metadata } from "next";import { LegalPage } from "@/components/marketing/LegalPage";import { legalBusiness } from "@/content/legal";export const metadata:Metadata={title:"Accessibility",description:"BREERO accessibility commitment.",alternates:{canonical:"/accessibility"}};export default function Page(){return <LegalPage title="Accessibility" updated="12 August 2026"><h2>Our approach</h2><p>BREERO aims for keyboard access, visible focus, readable contrast, semantic structure and clear error feedback.</p><h2>Known boundaries</h2><p>Third-party payment interfaces may have behavior controlled by the payment provider.</p><h2>Feedback</h2><p>Report an accessibility barrier to <a href={`mailto:${legalBusiness.supportEmail}`}>{legalBusiness.supportEmail}</a> with the page and task affected.</p></LegalPage>}
