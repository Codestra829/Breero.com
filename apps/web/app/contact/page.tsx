import type { Metadata } from "next"; import { StandardPage } from "@/components/marketing/StandardPage"; import { corePages } from "@/content/pages/core"; import { legalBusiness } from "@/content/legal";export const metadata:Metadata={title:"Contact BREERO",description:corePages.contact.description,alternates:{canonical:"/contact"}};export default function Page(){return <StandardPage content={corePages.contact}><section className="mk-cta mk-cta--warm"><div className="mk-container"><div><p className="mk-eyebrow">Customer and provider support</p><h2>Send us the details.</h2><p>Include your booking, request, or lead reference when one is available. Never email passwords or payment credentials.</p></div><a className="mk-button mk-button--primary" href={`mailto:${legalBusiness.supportEmail}`} data-cta="contact-support">Email support</a></div></section></StandardPage>}
