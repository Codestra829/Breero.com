import type { Metadata } from "next"; import Link from "next/link"; import { Footer,Header } from "../../components/marketplace/SiteChrome"; import { serviceCatalog } from "../../lib/booking-catalog";
export const metadata:Metadata={title:"Home services",description:"Browse cleaning, plumbing, electrical, handyman, garden, and appliance services from BREERO."};
export default function Services(){return <><Header/><main className="shell"><header className="catalog-head"><p className="eyebrow">Service marketplace</p><h1>What can we take off your list?</h1><p>Browse trusted help for repairs, upkeep, and home projects.</p><div className="chips" aria-label="Service categories"><span className="chip">All services</span><span className="chip">Cleaning</span><span className="chip">Repairs</span><span className="chip">Outdoor</span></div></header><section className="service-list" aria-label="Available services">{serviceCatalog.map(s=><Link className="service" href={`/services/${s.slug}`} key={s.id}><strong>{s.name}</strong><p>{s.description}</p><span>From €{s.base_price} · about {s.duration_minutes} min</span><span className="arrow">View service →</span></Link>)}</section><section className="section notice"><h2>Not sure which service fits?</h2><p>Start the guided booking flow and choose the closest match. You can add details before confirming.</p><Link className="button" href="/booking">Get started</Link></section></main><Footer/></>}
