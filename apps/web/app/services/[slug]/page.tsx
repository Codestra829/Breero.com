import type { Metadata } from "next"; import Link from "next/link"; import { notFound } from "next/navigation"; import { serviceBySlug,serviceCatalog } from "../../../lib/booking-catalog";
export function generateStaticParams(){return serviceCatalog.map(s=>({slug:s.slug}))} export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const s=serviceBySlug((await params).slug);return s?{title:s.name,description:`Book ${s.name.toLowerCase()} with BREERO. From €${s.base_price}.`}:{title:"Service not found"}}
export default async function Detail({params}:{params:Promise<{slug:string}>}){const s=serviceBySlug((await params).slug);if(!s)notFound();return <div className="shell"><section className="detail-hero"><div><p className="market-eyebrow">BREERO service</p><h1>{s.name}</h1><p>{s.description}</p><h2>What to expect</h2><ul><li>A professional matched to the service requested</li><li>Arrival within your selected time window</li><li>Clear review before booking and payment</li></ul><h2>What’s included</h2><p>Assessment, standard labour for the booked duration, and tidy handover of the work area.</p><h2>Not included</h2><p>Specialist materials, concealed defects, permits, or additional work not agreed through a quote.</p><h2>Frequently asked</h2><div className="faq"><details><summary>Is the starting price final?</summary><p>It covers the standard service shown. Your review step shows the booking price; additional work requires a separate quote.</p></details><details><summary>How long will it take?</summary><p>Most appointments take around {s.duration_minutes} minutes. The actual scope may change this.</p></details></div></div><aside className="price-box"><p className="market-eyebrow">Starting at</p><h2>€{s.base_price}</h2><p>Standard service pricing · approximately {s.duration_minutes} minutes</p><Link className="market-button" href={`/booking?service=${s.id}`}>Book {s.name.toLowerCase()}</Link><p><small>Address validation and live availability come next.</small></p></aside></section></div>}
