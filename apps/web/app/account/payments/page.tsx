import { Badge, Card, Price, ShieldIcon, Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@breero/ui";
import { AccountPageHeader } from "@/components/account/page-header";
import { formatDate, payments } from "@/lib/customer/data";

export const metadata={title:"Payment history"};
export default function PaymentsPage(){const total=payments.filter((p)=>p.status==="CAPTURED").reduce((sum,p)=>sum+p.captured_amount_minor,0);return <><AccountPageHeader eyebrow="Receipts & refunds" title="Payments" description="A customer-safe record of payments made through BREERO."/><div className="account-grid payment-summary"><Card className="account-col-4 metric-card"><span><ShieldIcon/></span><strong>{new Intl.NumberFormat("en-GB",{style:"currency",currency:"GBP"}).format(total/100)}</strong><p>Completed payments shown</p></Card><Card className="account-col-8"><div className="safe-payment-note"><ShieldIcon/><div><strong>Your payment details stay private</strong><p>We show amounts, booking references and statuses only—never card secrets, provider internals, professional compensation or BREERO margin.</p></div></div></Card><div className="account-col-12"><Table><TableHeader><TableRow><TableHead>Date</TableHead><TableHead>Booking</TableHead><TableHead>Method</TableHead><TableHead>Status</TableHead><TableHead>Amount</TableHead><TableHead><span className="br-sr-only">Receipt</span></TableHead></TableRow></TableHeader><TableBody>{payments.map((payment)=><TableRow key={payment.id}><TableCell>{formatDate(payment.created_at)}</TableCell><TableCell><a href={`/account/bookings/${payment.booking_id}`}>{payment.booking_id}</a></TableCell><TableCell>Card ending •••• 4242</TableCell><TableCell><Badge variant={payment.status==="CAPTURED"?"success":"neutral"}>{payment.status==="CAPTURED"?"Paid":"Refunded"}</Badge></TableCell><TableCell><Price amount={payment.amount_minor/100} currency={payment.currency}/></TableCell><TableCell><a href={`/account/payments/${payment.id}`}>Receipt</a></TableCell></TableRow>)}</TableBody></Table></div></div></>}
