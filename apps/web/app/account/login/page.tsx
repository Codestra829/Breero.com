import { AuthCard } from "@/components/account/auth-card";import { AuthForm } from "@/components/account/auth-form";export const metadata={title:"Sign in"};export default function Login(){return <AuthCard eyebrow="Welcome back" title="Sign in to BREERO" description="See your bookings, quotes and payments in one calm place." footer={<>New to BREERO? <a href="/account/register">Create an account</a></>}><AuthForm mode="login"/></AuthCard>}
