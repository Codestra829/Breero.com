import { notFound } from "next/navigation";
import { Badge, Card, Price } from "@breero/ui";
import { QuoteApproval } from "@/components/account/quote-approval";
import { bookings, formatDate, formatMoney, quotes } from "@/lib/customer/data";

export default async function QuoteDetail({params}:{params:Promise<{id:string}>}){const {id}=await params;const quote=quotes.find((item)=>item.id===id);if(!quote)notFound();const booking=bookings.find((item)=>item.id===quote.booking_id);const subtotal=quote.lines.reduce((sum,line)=>sum+line.total_amount_minor,0);return <><a className="account-back" href="/account/quotes">← Back to quotes</a><div className="detail-hero"><div><Badge variant={quote.status==="PENDING"?"warning":"success"}>{quote.status}</Badge><h1>{booking?.service??"Service quote"}</h1><p>Quote {quote.id} · Booking {quote.booking_id}</p></div><div className="detail-hero__amount"><small>Total including VAT</small><Price amount={quote.total_amount_minor/100} currency={quote.currency}/></div></div><div className="account-grid"><div className="account-col-8"><Card className="detail-section"><h2>Proposed work</h2><p>Replace the worn mixer tap cartridge, test the seals and confirm normal operation before leaving.</p><table className="quote-lines"><thead><tr><th>Description</th><th>Qty</th><th>Amount</th></tr></thead><tbody>{quote.lines.map((line)=><tr key={line.id}><td>{line.description}</td><td>{line.quantity}</td><td>{formatMoney(line.total_amount_minor,quote.currency)}</td></tr>)}</tbody></table><div className="quote-totals"><div><span>Subtotal</span><span>{formatMoney(subtotal,quote.currency)}</span></div><div><span>VAT</span><span>Included</span></div><div><span>Total</span><strong>{formatMoney(quote.total_amount_minor,quote.currency)}</strong></div></div></Card><Card className="detail-section quote-detail-terms"><h2>Terms</h2><p className="quote-terms">{quote.terms}</p>{quote.expires_at&&<p><strong>{quote.status==="PENDING"?"Valid until":"Valid through"}:</strong> {formatDate(quote.expires_at,{weekday:"long",day:"numeric",month:"long",year:"numeric"})}</p>}</Card></div><Card className="account-col-4 approval-panel">{quote.status==="PENDING"?<QuoteApproval quoteId={quote.id} requiresPayment={booking?.payment_required}/>:<div className="approval-success"><Badge variant="success">Approved</Badge><h2>No action needed</h2><p>This quote has already been approved.</p><a href={`/account/bookings/${quote.booking_id}`}>View booking →</a></div>}</Card></div></>}
