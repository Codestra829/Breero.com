import { Badge, Button, Card, HomeIcon, IconButton } from "@breero/ui";
import { AccountPageHeader } from "@/components/account/page-header";
import { addresses } from "@/lib/customer/data";
export const metadata={title:"Saved addresses"};export default function AddressesPage(){return <><AccountPageHeader eyebrow="Your places" title="Saved addresses" description="Choose where you need help more quickly when you book." action={<Button size="sm">Add address</Button>}/><Card><div className="address-list">{addresses.map((address)=><article className="address-card" key={address.id}><span><HomeIcon/></span><div><strong>{address.label} {address.default&&<Badge variant="brand">Default</Badge>}</strong><small>{address.line1}, {address.city}, {address.postalCode}</small></div><IconButton label={`Edit ${address.label}`} variant="outline">•••</IconButton></article>)}</div></Card><p className="safe-payment-note address-note"><HomeIcon size={17}/>Address creation and editing currently use the customer adapter until backend saved-address endpoints are available.</p></>}
