import { Badge, Card, HomeIcon, IconButton } from "@breero/ui";
import { AccountPageHeader } from "@/components/account/page-header";
import { addresses } from "@/lib/customer/data";
export const metadata={title:"Saved addresses"};export default function AddressesPage(){return <><AccountPageHeader eyebrow="Your places" title="Saved addresses" description="Choose where you need help more quickly when you book."/><Card><div className="address-list">{addresses.map((address)=><article className="address-card" key={address.id}><span><HomeIcon/></span><div><strong>{address.label} {address.default&&<Badge variant="brand">Default</Badge>}</strong><small>{address.line1}, {address.city}, {address.postalCode}</small></div><IconButton label={`Address options for ${address.label}`} variant="outline" disabled>•••</IconButton></article>)}</div></Card></>}
