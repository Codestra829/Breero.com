import Image from "next/image"; import Link from "next/link"; import type { MarketingService } from "@/content/services"; import { TrustBar } from "./TrustBar"; import { FAQ } from "./FAQ"; import { CTASection } from "./CTASection";
export function ServicePageTemplate({service}:{service:MarketingService}){const booking=service.bookingServiceId?`/booking?service=${service.bookingServiceId}`:"/booking";return <><div className="mk-breadcrumb mk-container"><Link href="/services">Services</Link><span>/</span><span>{service.name}</span></div><section className="mk-service-hero"><div className="mk-container mk-service-hero__grid"><div><p className="mk-eyebrow">BREERO {service.name.toLowerCase()}</p><h1>{service.name}, without the runaround.</h1><p className="mk-lead">{service.promise} {service.description}</p><div className="mk-actions"><Link className="mk-button mk-button--primary" href={booking} data-cta={`service-book-${service.slug}`}>{service.bookingServiceId?"Book now":"Check availability"}</Link><a className="mk-button mk-button--secondary" href="#included">What’s included</a></div>{!service.bookingServiceId&&<p className="mk-availability-note">This service is informational until live catalog availability is confirmed for your address.</p>}</div><div className="mk-service-hero__image"><Image src={service.image.src} alt={service.image.alt} fill priority sizes="(max-width: 768px) 100vw, 48vw" className="mk-cover"/></div></div></section><TrustBar/><section className="mk-section"><div className="mk-container mk-three"><article><p className="mk-eyebrow">Common problems</p><h2>When to get help</h2><ul>{service.problems.map(item=><li key={item}>{item}</li>)}</ul></article><article id="included"><p className="mk-eyebrow">What’s included</p><h2>A professional visit</h2><ul>{service.included.map(item=><li key={item}>{item}</li>)}</ul></article><article><p className="mk-eyebrow">Before the visit</p><h2>Simple preparation</h2><p>{service.preparation}</p></article></div></section><section className="mk-section mk-section--sky"><div className="mk-container mk-narrow"><header className="mk-heading"><p className="mk-eyebrow">Pricing and process</p><h2>Clear before you commit.</h2><p>Availability, scope and the authoritative amount and currency are shown during the booking flow. If extra work is needed later, it requires a separate quote and approval.</p></header><FAQ limit={5}/></div></section><CTASection title={`Need ${service.name.toLowerCase()} help?`} text="Tell us what is happening and check live coverage for your address."/></>}
