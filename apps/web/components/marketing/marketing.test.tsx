import { render,screen } from "@testing-library/react";import { describe,expect,it } from "vitest";import { Hero } from "./Hero";import { ServiceCard } from "./ServiceCard";import { FAQ } from "./FAQ";import { marketingServices } from "@/content/services";
describe("marketing system",()=>{it("uses the canonical booking CTA",()=>{render(<Hero title="Home services" description="Trusted help" image={{src:"/images/hero/home-hero.webp",alt:"Professional arriving"}}/>);expect(screen.getByRole("link",{name:"Book a service"})).toHaveAttribute("href","/booking")});it("links service cards to public service pages",()=>{render(<ServiceCard service={marketingServices[0]}/>);expect(screen.getByRole("link")).toHaveAttribute("href","/services/plumbing")});it("renders accessible FAQ controls",()=>{render(<FAQ limit={2}/>);expect(screen.getAllByText(/How|pricing/i).length).toBeGreaterThan(0)})});
