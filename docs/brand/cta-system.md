# CTA system

Common labels and routes live in `apps/web/content/cta.ts`. A section gets one primary action and
at most one secondary action. The primary public action enters `/booking`; service pages pass only
known catalog IDs. Partner interest remains an informational/contact path. `data-cta` values are
semantic and contain no personal data.
